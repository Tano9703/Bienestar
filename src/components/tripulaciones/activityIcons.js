import { BrainCircuit, Users, MessageSquare, FlaskConical, ClipboardCheck, FileText } from 'lucide-react';

export const activityIconMap = {
  BrainCircuit,
  Users,
  MessageSquare,
  FlaskConical,
  ClipboardCheck,
  FileText,
};